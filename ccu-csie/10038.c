#include <stdio.h>

int main(){
    int n,i=0;
    scanf("%d",&n);
    int arr[n-1];
    while(scanf("%d",&arr[i])!=EOF){
        if(i==0){
            continue;
        }
        else{
            
        }
    }
}