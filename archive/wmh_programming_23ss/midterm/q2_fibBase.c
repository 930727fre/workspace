#include <stdio.h>

int main(){
    int fib[45]={1,1}, n, flag, num;
    for(int i=2;i<45;i++){
        fib[i]=fib[i-1]+fib[i-2];
    }
    scanf("%d",&n);
    for(int i=0;i<n;i++){
        scanf("%d",&num);
        for(int i=45;i>)
    }
}